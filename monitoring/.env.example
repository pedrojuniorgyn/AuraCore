# =============================================================================
# MONITORING STACK - ENVIRONMENT VARIABLES
# =============================================================================
# Copy to .env and fill in values before running docker-compose up
# =============================================================================

# ── Grafana ──────────────────────────────────────────────────────────────────
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=auracore2026
GRAFANA_ROOT_URL=http://localhost:3001

# ── Alertmanager SMTP (Google Workspace) ─────────────────────────────────────
# Gmail / Google Workspace SMTP settings for email alerts.
#
# For Google Workspace:
#   1. Use a service account or app-specific password (NOT your login password)
#   2. Enable "Less secure apps" or use App Passwords:
#      https://myaccount.google.com/apppasswords
#   3. SMTP_USER = your Google Workspace email (e.g. alerts@auracore.cloud)
#   4. SMTP_PASS = the generated App Password (16 chars, no spaces)
#
SMTP_USER=alerts@auracore.cloud
SMTP_PASS=
SMTP_FROM=alerts@auracore.cloud
ALERT_EMAIL_TO=admin@auracore.cloud
