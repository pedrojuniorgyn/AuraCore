---
description: Regras MCP obrigatórias do AuraCore - Arquitetura 100% DDD/Hexagonal, TypeScript strict, padrões de código. Consultar contratos e verificar com grep ANTES de codificar. Zero bugs por falta de verificação.
globs: "**/*.ts,**/*.tsx"
alwaysApply: true
---

# AURA CORE - REGRAS MCP OBRIGATÓRIAS v2.0

**Versão:** 2.0.0  
**Data:** 06/01/2026  
**Arquitetura:** 100% DDD/Hexagonal (ADR-0015)

---

## 🚨 REGRA FUNDAMENTAL (INVIOLÁVEL)

ANTES de implementar QUALQUER código, o agente DEVE:

### 1. Consultar Contratos MCP

```
Tool: get_contract
Args: { "contract_id": "verify-before-code" }

Tool: get_contract
Args: { "contract_id": "code-consistency" }

Tool: get_contract
Args: { "contract_id": "type-safety" }

Tool: get_contract
Args: { "contract_id": "mcp-enforcement-rules" }

Tool: get_contract
Args: { "contract_id": "infrastructure-layer" }

Tool: get_contract
Args: { "contract_id": "known-bugs-registry" }

Tool: get_contract
Args: { "contract_id": "architecture-layers" }

Tool: get_contract
Args: { "contract_id": "schema-pattern" }
```

### 2. Buscar Padrões Relacionados

```
Tool: search_patterns
Args: { "query": "[tema da tarefa]" }
```

### 3. Verificar com GREP Antes de Usar (OBRIGATÓRIO)

```bash
# Interface
grep -rn "interface NomeInterface" src/

# Enum
grep -A10 "NomeEnum" src/lib/db/schema.ts

# Método
grep -n "nomeMetodo(" src/modules/

# Propriedade de contexto
grep -n "propriedade:" src/lib/auth/context.ts

# SpedDocumentType (para SPED)
grep -A10 "SpedDocumentType" src/modules/fiscal/domain/value-objects/SpedDocument.ts

# Entity existente
grep -rn "class NomeEntity" src/modules/

# Value Object existente
grep -rn "class NomeVO extends ValueObject" src/modules/

# Repository interface
grep -rn "interface I.*Repository" src/modules/
```

### 4. Confirmar Leitura

Responder: **"Contratos MCP consultados. Verificações grep realizadas. Iniciando implementação."**

**NUNCA pular esta etapa. Se não conseguir consultar, informar o usuário.**

---

## 📐 ARQUITETURA - REGRAS INVIOLÁVEIS (ARCH-001 a ARCH-015)

### Dependências entre Camadas

| Regra | Descrição | Prioridade |
|-------|-----------|------------|
| ARCH-001 | Domain NÃO importa de Application | CRÍTICA |
| ARCH-002 | Domain NÃO importa de Infrastructure | CRÍTICA |
| ARCH-003 | Domain NÃO importa bibliotecas externas (drizzle, axios) | CRÍTICA |
| ARCH-004 | Domain NÃO importa módulos Node.js (fs, path, crypto) | CRÍTICA |
| ARCH-005 | Application NÃO importa de Infrastructure (exceto via DI) | ALTA |
| ARCH-006 | Dependências SEMPRE apontam para Domain (inward) | CRÍTICA |
| ARCH-007 | Entities têm comportamento (não são anêmicas) | ALTA |
| ARCH-008 | Value Objects são imutáveis (Object.freeze) | ALTA |
| ARCH-009 | Domain Services são stateless (métodos estáticos) | ALTA |
| ARCH-010 | Use Cases implementam interface de domain/ports/input/ | ALTA |
| ARCH-011 | Repositories implementam interface de domain/ports/output/ | ALTA |
| ARCH-012 | Commands em application/commands/ | MÉDIA |
| ARCH-013 | Queries em application/queries/ | MÉDIA |
| ARCH-014 | Mappers têm toDomain() e toPersistence() | ALTA |
| ARCH-015 | toDomain() usa reconstitute(), não create() | ALTA |

### Estrutura de Pastas Obrigatória

```
src/modules/{module}/
├── domain/
│   ├── entities/
│   ├── aggregates/         # ← Aggregates Root (opcional)
│   ├── value-objects/
│   ├── services/           # ← OBRIGATÓRIO
│   ├── events/
│   ├── errors/
│   └── ports/
│       ├── input/          # ← OBRIGATÓRIO
│       └── output/         # ← OBRIGATÓRIO
├── application/
│   ├── commands/           # ← OBRIGATÓRIO
│   ├── queries/            # ← OBRIGATÓRIO
│   └── dtos/
└── infrastructure/
    ├── persistence/
    │   ├── repositories/
    │   ├── mappers/
    │   └── schemas/        # 1 arquivo por tabela
    └── di/
```

**Nota sobre Aggregates:**
- Aggregates simples podem ficar em `entities/` 
- Aggregates complexos com entidades filhas devem ficar em `aggregates/`
- Aggregate Root SEMPRE estende `AggregateRoot<string>`

---

## 🗄️ BANCO DE DADOS - SCHEMA PATTERN (SCHEMA-001 a SCHEMA-010)

| Regra | Descrição | Prioridade |
|-------|-----------|------------|
| SCHEMA-001 | Um arquivo schema por tabela | ALTA |
| SCHEMA-002 | Nome: {entity}.schema.ts (lowercase) | MÉDIA |
| SCHEMA-003 | Índice composto: (organizationId, branchId) | CRÍTICA |
| SCHEMA-004 | Índices para filtros frequentes | ALTA |
| SCHEMA-005 | Campos: createdAt, updatedAt obrigatórios | ALTA |
| SCHEMA-006 | Soft delete: deletedAt nullable | ALTA |
| SCHEMA-007 | Money = 2 colunas (amount + currency) | ALTA |
| SCHEMA-008 | Export const tableName = mssqlTable(...) | MÉDIA |
| SCHEMA-009 | Tipos inferidos: $inferSelect, $inferInsert | ALTA |
| SCHEMA-010 | Índice único para chaves naturais | MÉDIA |

---

## 🔷 ENTITY PATTERN (ENTITY-001 a ENTITY-012)

| Regra | Descrição |
|-------|-----------|
| ENTITY-001 | Extends AggregateRoot<string> ou Entity<string> |
| ENTITY-002 | Factory method create() com validações |
| ENTITY-003 | Método reconstitute() sem validações |
| ENTITY-004 | create() retorna Result<Entity, string> |
| ENTITY-005 | NUNCA setters públicos |
| ENTITY-006 | Getters para propriedades |
| ENTITY-007 | Métodos de negócio (approve, cancel, etc) |
| ENTITY-008 | Validar invariantes em create() |
| ENTITY-009 | Emitir Domain Events em mudanças de estado |
| ENTITY-010 | ID gerado internamente (crypto.randomUUID) |
| ENTITY-011 | Multi-tenancy: organizationId + branchId |
| ENTITY-012 | Timestamps: createdAt, updatedAt |

---

## 🔶 VALUE OBJECT PATTERN (VO-001 a VO-010)

| Regra | Descrição |
|-------|-----------|
| VO-001 | Extends ValueObject<Props> |
| VO-002 | Props extends Record<string, unknown> |
| VO-003 | Constructor privado |
| VO-004 | Factory method create() estático |
| VO-005 | Object.freeze no constructor |
| VO-006 | Imutável (sem métodos que alteram estado) |
| VO-007 | Comparação por valor (equals) |
| VO-008 | Validações em create() |
| VO-009 | Retorna Result<VO, string> |
| VO-010 | toString() para debug |

---

## 🔵 DOMAIN SERVICE PATTERN (DOMAIN-SVC-001 a DOMAIN-SVC-010)

| Regra | Descrição |
|-------|-----------|
| DOMAIN-SVC-001 | 100% Stateless (métodos estáticos) |
| DOMAIN-SVC-002 | Constructor privado (impede instanciação) |
| DOMAIN-SVC-003 | Retorna Result<T, string> |
| DOMAIN-SVC-004 | NUNCA faz throw |
| DOMAIN-SVC-005 | ZERO dependências de infraestrutura |
| DOMAIN-SVC-006 | ZERO acesso a banco de dados |
| DOMAIN-SVC-007 | 100% testável sem mocks |
| DOMAIN-SVC-008 | Lógica de negócio PURA |
| DOMAIN-SVC-009 | Localização: domain/services/ |
| DOMAIN-SVC-010 | Nome descritivo: {Ação}Calculator, {Ação}Validator |

---

## 🟣 USE CASE PATTERN (USE-CASE-001 a USE-CASE-015)

| Regra | Descrição |
|-------|-----------|
| USE-CASE-001 | Commands em application/commands/ |
| USE-CASE-002 | Queries em application/queries/ |
| USE-CASE-003 | Implementa interface de domain/ports/input/ |
| USE-CASE-004 | Método único: execute() |
| USE-CASE-005 | Recebe ExecutionContext (userId, orgId, branchId) |
| USE-CASE-006 | Retorna Promise<Result<Output, string>> |
| USE-CASE-007 | Valida input (Zod ou manual) |
| USE-CASE-008 | Verifica multi-tenancy |
| USE-CASE-009 | Delega lógica para Domain |
| USE-CASE-010 | Usa DI para dependencies |
| USE-CASE-011 | @injectable() decorator |
| USE-CASE-012 | Commands podem modificar estado |
| USE-CASE-013 | Queries NUNCA modificam estado |
| USE-CASE-014 | Publica Domain Events |
| USE-CASE-015 | Gerencia transações |

---

## 🟢 REPOSITORY PATTERN (REPO-001 a REPO-012)

| Regra | Descrição |
|-------|-----------|
| REPO-001 | Interface em domain/ports/output/ |
| REPO-002 | Implementação em infrastructure/persistence/repositories/ |
| REPO-003 | Implementa interface do domain |
| REPO-004 | Usa Mapper para conversão |
| REPO-005 | TODA query filtra organizationId + branchId |
| REPO-006 | Soft delete: filtrar deletedAt IS NULL |
| REPO-007 | UPSERT com onConflictDoUpdate |
| REPO-008 | Transações para operações múltiplas |
| REPO-009 | Paginação obrigatória em findMany |
| REPO-010 | Retorna Domain Entity, não row do banco |
| REPO-011 | @injectable() decorator |
| REPO-012 | Nome: Drizzle{Entity}Repository |

---

## 🟡 MAPPER PATTERN (MAPPER-001 a MAPPER-008)

| Regra | Descrição |
|-------|-----------|
| MAPPER-001 | Localização: infrastructure/persistence/mappers/ |
| MAPPER-002 | Método toDomain(row): Result<Entity, string> |
| MAPPER-003 | Método toPersistence(entity): Insert |
| MAPPER-004 | toDomain usa reconstitute(), NUNCA create() |
| MAPPER-005 | Money → 2 campos (amount, currency) |
| MAPPER-006 | Validar conversão de Value Objects |
| MAPPER-007 | Classe com métodos estáticos |
| MAPPER-008 | Nome: {Entity}Mapper |

---

## 🔴 REGRAS CRÍTICAS DE CÓDIGO

### TypeScript Strict

- ❌ NUNCA usar `any` (usar `unknown` ou tipo explícito)
- ❌ NUNCA usar `@ts-ignore`
- ❌ NUNCA usar `as any`
- ✅ SEMPRE tipar retornos de função
- ✅ SEMPRE tipar parâmetros

### Validação de Input

- ✅ SEMPRE validar input com Zod em rotas API
- ✅ SEMPRE usar .trim() antes de validar comprimento de strings
- ✅ SEMPRE verificar Result.isFail() antes de acessar .value

### Result Pattern

- ✅ SEMPRE verificar Result.isFail() antes de acessar .value
- ✅ SEMPRE retornar Result em operações que podem falhar
- ❌ NUNCA fazer throw para erros de negócio
- ❌ NUNCA acessar .value sem verificar

### Multi-Tenancy

- ✅ SEMPRE filtrar por organizationId + branchId
- ❌ branchId NUNCA pode ser opcional em filters
- ✅ Índice composto obrigatório no schema

### Transações SQL

- ✅ SEMPRE usar transação em operações multi-step
- ✅ Usar withMssqlTransaction helper

### Mapeamento Domain-Persistence

- ✅ Schema DEVE espelhar Domain COMPLETO
- ✅ Money = 2 campos (amount + currency)
- ✅ Campos opcionais = .nullable()
- ✅ toDomain usa reconstitute(), NÃO create()
- ✅ UPDATE com TODOS os campos mutáveis

### Funções Utilitárias

- ✅ Quando criar função utilitária: USAR em TODOS os lugares
- ✅ REMOVER código duplicado que a função substitui
- ❌ NUNCA deixar lógica hardcoded quando existe função centralizada

### Máquina de Estados

- ✅ Transições que diferem por tipo DEVEM ter máquinas separadas
- ❌ NUNCA modificar máquina global para caso específico
- ✅ SEMPRE usar canTransitionTo(from, to, documentType)

---

## ✅ VALIDAÇÕES PRÉ-COMMIT OBRIGATÓRIAS

ANTES de commit, executar TODOS:

```bash
# 1. TypeScript - deve retornar 0 erros
npx tsc --noEmit

# 2. Testes - todos devem passar
npm test -- --run

# 3. Buscar any - deve retornar 0 resultados
grep -r 'as any' src/

# 4. Verificar status git
git status
```

### 5. Verificar Issues do Cursor (OBRIGATÓRIO)

```
Tool: check_cursor_issues
Args: {}
```

Se QUALQUER verificação falhar, corrigir antes de commit.

---

## 📋 FLUXO DE TRABALHO OBRIGATÓRIO

### Implementação

1. Consultar contratos MCP (Tool: get_contract)
2. Buscar padrões (Tool: search_patterns)
3. Verificar com grep
4. Confirmar leitura
5. Implementar seguindo regras
6. Testar localmente

### Pré-Commit

```
Tool: check_cursor_issues
Args: {}
```

1. Se issues > 0: corrigir
2. Se issues = 0: fazer commit

### Pós-Commit

```
Tool: check_cursor_issues
Args: {}
```

1. Se issues > 0: corrigir + registrar correção + git commit --amend
2. Se issues = 0: reportar para aprovação

### Registrar Correção (quando aplicável)

```
Tool: register_correction
Args: {
  "epic": "[épico atual]",
  "error_description": "[descrição clara do erro]",
  "correction_applied": "[como foi corrigido]",
  "files_affected": ["arquivo1.ts", "arquivo2.ts"]
}
```

### Push

1. NUNCA fazer push sem aprovação explícita do usuário
2. Aguardar mensagem "APROVADO - FAZER PUSH"
3. Verificar git status antes do push

---

## 🛡️ PREVENÇÃO DE GAPS (PREVENT-001 a PREVENT-008)

| Regra | Descrição |
|-------|-----------|
| PREVENT-001 | Todo novo módulo DEVE seguir template |
| PREVENT-002 | Todo Use Case em commands/ ou queries/ |
| PREVENT-003 | Todo service DEVE estar em um módulo |
| PREVENT-004 | Código em src/services/ é PROIBIDO |
| PREVENT-005 | Domain NUNCA importa de fora |
| PREVENT-006 | branchId SEMPRE obrigatório |
| PREVENT-007 | Repository SEMPRE usa Mapper |
| PREVENT-008 | Entity SEMPRE tem create() e reconstitute() |

---

## ⛔ PROIBIÇÕES ABSOLUTAS

- ❌ NUNCA usar `any` (nem em testes)
- ❌ NUNCA fazer push sem aprovação
- ❌ NUNCA criar função utilitária sem usá-la em todos os lugares
- ❌ NUNCA modificar máquina de estados global para caso específico
- ❌ NUNCA validar string sem .trim()
- ❌ NUNCA acessar .value sem verificar Result.isFail()
- ❌ NUNCA deixar testes skipped
- ❌ NUNCA usar placeholders como 'TEMP' ou 'TODO' em código de produção
- ❌ NUNCA fazer commits incrementais de correção (usar --amend)

---

## 📊 REGRA DE IMPACTO CRUZADO

Antes de modificar método compartilhado (authorize, cancel, submit, etc):

1. Listar TODOS os document types que usam esse método
2. Verificar se a mudança afeta cada tipo diferentemente
3. Adicionar condicional por tipo se necessário
4. Usar funções centralizadas ao invés de hardcode

---

## 🇧🇷 CONTEXTO BRASIL (ERP Logístico)

### Legislação Fiscal

- ICMS (Lei Complementar 87/96 - Lei Kandir)
- PIS/COFINS (Leis 10.637/02 e 10.833/03)
- SPED Fiscal, SPED Contribuições, SPED ECD
- CTe (Conhecimento de Transporte Eletrônico)
- NFe (Nota Fiscal Eletrônica)
- MDFe (Manifesto de Documentos Fiscais)
- NFS-e (Nota Fiscal de Serviços Eletrônica)

### Reforma Tributária 2026

- IBS (Imposto sobre Bens e Serviços)
- CBS (Contribuição sobre Bens e Serviços)
- IS (Imposto Seletivo)

### Retenções na Fonte

- IRRF (1.5%)
- PIS (0.65%)
- COFINS (3%)
- CSLL (1%)
- ISS (2-5%)

---

## 🔧 STACK TÉCNICA

- **Backend:** Next.js 15 (App Router), TypeScript, Drizzle ORM, SQL Server 2022
- **Frontend:** React 19, Refine, AG Grid, Shadcn/UI
- **Testes:** Vitest
- **DI:** tsyringe
- **Deploy:** Coolify

---

## 📊 FORMATO DE RELATÓRIO FINAL

Ao concluir implementação, reportar:

```markdown
## TAREFA CONCLUÍDA

### Verificações MCP
- check_cursor_issues (pré-commit): ✅/❌
- check_cursor_issues (pós-commit): ✅/❌

### Verificações Código
- npx tsc --noEmit: ✅ 0 erros
- npm test -- --run: ✅ X/X passando
- grep 'as any': ✅ 0 resultados

### Commits
- Hash: [hash]
- Mensagem: [mensagem]

### Push
- Status: ✅ Realizado / ⏳ Pendente

### Correções Registradas
- [LC-XXXXX]: [descrição] (se houver)
```

---

## 📁 ARQUIVOS CRÍTICOS (CUIDADO EXTRA)

Modificações nesses arquivos exigem testes extras e revisão cuidadosa:

1. `accounting-engine.ts` - Contabilização
2. `financial-title-generator.ts` - Títulos financeiros
3. `sped-fiscal-generator.ts` - SPED (multa R$ 5.000+)
4. `sped-ecd-generator.ts` - SPED Contábil
5. `sped-contributions-generator.ts` - SPED PIS/COFINS
6. `FiscalDocument.ts` - Documento fiscal base
7. `DocumentType.ts` - Máquina de estados

---

## 📝 TEMPLATES DE CÓDIGO

### Template: Entity

```typescript
// src/modules/{module}/domain/entities/{Entity}.ts
import { AggregateRoot, Result } from '@/shared/domain';

interface {Entity}Props {
  organizationId: number;
  branchId: number;
  // campos de negócio
  createdAt: Date;
  updatedAt: Date;
}

export class {Entity} extends AggregateRoot<string> {
  private constructor(id: string, private readonly props: {Entity}Props) {
    super(id);
  }

  // Getters
  get organizationId(): number { return this.props.organizationId; }
  get branchId(): number { return this.props.branchId; }

  // Factory: create() COM validações
  static create(props: Create{Entity}Props): Result<{Entity}, string> {
    if (!props.organizationId) return Result.fail('organizationId obrigatório');
    if (!props.branchId) return Result.fail('branchId obrigatório');
    
    const id = globalThis.crypto.randomUUID();
    const now = new Date();
    
    return Result.ok(new {Entity}(id, { ...props, createdAt: now, updatedAt: now }));
  }

  // Factory: reconstitute() SEM validações (para Mapper)
  static reconstitute(props: {Entity}Props & { id: string }): Result<{Entity}, string> {
    return Result.ok(new {Entity}(props.id, props));
  }

  // Métodos de negócio
  approve(approvedBy: string): Result<void, string> {
    // lógica de negócio
    this.addDomainEvent(new {Entity}ApprovedEvent(this.id, approvedBy));
    return Result.ok(undefined);
  }
}
```

### Template: Value Object

```typescript
// src/modules/{module}/domain/value-objects/{VO}.ts
import { ValueObject, Result } from '@/shared/domain';

interface {VO}Props extends Record<string, unknown> {
  value: string;
}

export class {VO} extends ValueObject<{VO}Props> {
  private constructor(props: {VO}Props) {
    super(props); // Object.freeze aplicado
  }

  get value(): string { return this.props.value; }

  static create(value: string): Result<{VO}, string> {
    const trimmed = value.trim();
    if (!trimmed) return Result.fail('{VO} não pode ser vazio');
    return Result.ok(new {VO}({ value: trimmed }));
  }
}
```

### Template: Repository Interface

```typescript
// src/modules/{module}/domain/ports/output/I{Entity}Repository.ts
import type { {Entity} } from '../../entities/{Entity}';

export interface I{Entity}Repository {
  findById(id: string, organizationId: number, branchId: number): Promise<{Entity} | null>;
  findMany(filter: {Entity}Filter): Promise<{ items: {Entity}[]; total: number }>;
  save(entity: {Entity}): Promise<void>;
  delete(id: string, organizationId: number, branchId: number): Promise<void>;
}

export interface {Entity}Filter {
  organizationId: number;
  branchId: number; // NUNCA opcional
  status?: string;
  page?: number;
  pageSize?: number;
}
```

### Template: Mapper

```typescript
// src/modules/{module}/infrastructure/persistence/mappers/{Entity}Mapper.ts
import { Result } from '@/shared/domain';
import { {Entity} } from '../../../domain/entities/{Entity}';
import type { {Entity}Row, {Entity}Insert } from '../schemas/{entity}.schema';

export class {Entity}Mapper {
  // DB → Domain (usa reconstitute, NUNCA create)
  static toDomain(row: {Entity}Row): Result<{Entity}, string> {
    return {Entity}.reconstitute({
      id: row.id,
      organizationId: row.organizationId,
      branchId: row.branchId,
      // ... campos
      createdAt: row.createdAt,
      updatedAt: row.updatedAt,
    });
  }

  // Domain → DB
  static toPersistence(entity: {Entity}): {Entity}Insert {
    return {
      id: entity.id,
      organizationId: entity.organizationId,
      branchId: entity.branchId,
      // ... campos
      createdAt: entity.createdAt,
      updatedAt: entity.updatedAt,
    };
  }
}
```

### Template: Schema

```typescript
// src/modules/{module}/infrastructure/persistence/schemas/{entity}.schema.ts
import { int, varchar, decimal, datetime2, index } from 'drizzle-orm/mssql-core';
import { mssqlTable } from '@/shared/infrastructure/database/table-creator';

export const {entity}Table = mssqlTable(
  '{entity_snake_case}',
  {
    id: varchar('id', { length: 36 }).primaryKey(),
    organizationId: int('organization_id').notNull(),
    branchId: int('branch_id').notNull(),
    
    // Money = 2 colunas
    amount: decimal('amount', { precision: 18, scale: 2 }).notNull(),
    amountCurrency: varchar('amount_currency', { length: 3 }).notNull().default('BRL'),
    
    // Auditoria
    createdAt: datetime2('created_at').notNull().defaultNow(),
    updatedAt: datetime2('updated_at').notNull().defaultNow(),
    deletedAt: datetime2('deleted_at'), // Soft delete
  },
  (table) => ({
    // Índice COMPOSTO obrigatório
    tenantIdx: index('idx_{entity}_tenant').on(table.organizationId, table.branchId),
  })
);

export type {Entity}Row = typeof {entity}Table.$inferSelect;
export type {Entity}Insert = typeof {entity}Table.$inferInsert;
```

---

## 🇧🇷 CONTEXTO BRASIL - ERP LOGÍSTICO (EXPANDIDO)

### Legislação Fiscal Detalhada
- **ICMS:** Lei Complementar 87/96 (Lei Kandir) - Imposto estadual sobre circulação de mercadorias
- **PIS/COFINS:** Leis 10.637/02 e 10.833/03 - Contribuições federais sobre faturamento
- **SPED:** Sistema Público de Escrituração Digital
  - SPED Fiscal (EFD-ICMS/IPI)
  - SPED Contribuições (EFD-Contribuições PIS/COFINS)
  - SPED ECD (Escrituração Contábil Digital)
- **Documentos Eletrônicos:**
  - NFe (Nota Fiscal Eletrônica) - Modelo 55
  - CTe (Conhecimento de Transporte Eletrônico) - Modelo 57
  - MDFe (Manifesto de Documentos Fiscais Eletrônicos)
  - NFS-e (Nota Fiscal de Serviços Eletrônica)

### Reforma Tributária 2026 (CRÍTICO)
- **IBS:** Imposto sobre Bens e Serviços (estadual+municipal, substitui ICMS+ISS)
- **CBS:** Contribuição sobre Bens e Serviços (federal, substitui PIS/COFINS)
- **IS:** Imposto Seletivo (produtos específicos: cigarros, bebidas, etc)

### Retenções na Fonte (Lei 10.833/03)
| Tributo | Alíquota | Base Legal |
|---------|----------|------------|
| IRRF | 1.5% | Art. 64 Lei 9.430/96 |
| PIS | 0.65% | Art. 30 Lei 10.833/03 |
| COFINS | 3.0% | Art. 30 Lei 10.833/03 |
| CSLL | 1.0% | Art. 30 Lei 10.833/03 |
| ISS | 2-5% | LC 116/03 |

### Arquivos Críticos (Multas SEFAZ/RFB)
| Arquivo | Módulo | Risco | Multa Mínima | Base Legal |
|---------|--------|-------|--------------|------------|
| sped-fiscal-generator.ts | Fiscal | SPED Fiscal | R$ 5.000/mês | Art. 12 Lei 8.218/91 |
| sped-ecd-generator.ts | Accounting | SPED ECD | R$ 5.000/mês | Art. 12 Lei 8.218/91 |
| sped-contributions-generator.ts | Fiscal | EFD-Contribuições | R$ 5.000/mês | Art. 12 Lei 8.218/91 |
| FiscalDocument.ts | Fiscal | NFe/CTe inválida | R$ 5.000/doc | Art. 57 Lei 8.383/91 |
| TaxCalculator.ts | Fiscal | Cálculo incorreto | Crédito tributário | Variável |
| accounting-engine.ts | Accounting | Contabilização | Auditoria | IN RFB 1.700/17 |
| financial-title-generator.ts | Financial | Títulos duplicados | R$ 10.000+ | Art. 172 CTN |

---

## 📚 REFERÊNCIAS BIBLIOGRÁFICAS

### Arquitetura de Software
1. **Evans, E.** (2003). *Domain-Driven Design: Tackling Complexity in the Heart of Software*. Addison-Wesley Professional. ISBN: 978-0321125217.
2. **Vernon, V.** (2013). *Implementing Domain-Driven Design*. Addison-Wesley Professional. ISBN: 978-0321834577.
3. **Cockburn, A.** (2005). *Hexagonal Architecture (Ports and Adapters)*. Disponível em: alistair.cockburn.us/hexagonal-architecture/.
4. **Martin, R. C.** (2017). *Clean Architecture: A Craftsman's Guide to Software Structure and Design*. Prentice Hall. ISBN: 978-0134494166.
5. **Fowler, M.** (2002). *Patterns of Enterprise Application Architecture*. Addison-Wesley Professional. ISBN: 978-0321127426.

### Legislação Brasileira
6. **Brasil.** Lei Complementar nº 87, de 13 de setembro de 1996. Lei Kandir (ICMS).
7. **Brasil.** Lei nº 10.637, de 30 de dezembro de 2002. PIS não-cumulativo.
8. **Brasil.** Lei nº 10.833, de 29 de dezembro de 2003. COFINS não-cumulativo.
9. **Brasil.** Instrução Normativa RFB nº 1.774, de 22 de dezembro de 2017. SPED.
10. **Brasil.** Emenda Constitucional nº 132, de 20 de dezembro de 2023. Reforma Tributária.

---

## 📊 FORMATO DE RELATÓRIO FINAL (OBRIGATÓRIO)

Após cada tarefa, o agente DEVE produzir:

```markdown
## TAREFA CONCLUÍDA

### Verificações MCP
- check_cursor_issues (pré-commit): ✅ 0 issues / ❌ X issues
- check_cursor_issues (pós-commit): ✅ 0 issues / ❌ X issues

### Verificações Código
- npx tsc --noEmit: ✅ 0 erros
- npm test -- --run: ✅ X/X passando
- grep 'as any': ✅ 0 resultados

### Padrões Aplicados
- [ ] ARCH: Dependências inward (Cockburn, 2005)
- [ ] ENTITY: create() + reconstitute() (Evans, 2003)
- [ ] REPO: Mapper com toDomain/toPersistence (Vernon, 2013)
- [ ] SCHEMA: Índice composto multi-tenancy

### Commits
- Hash: [hash]
- Mensagem: [mensagem]

### Correções Registradas
- [LC-XXXXX]: [descrição] (se houver)

### Push
- Status: ⏳ Aguardando aprovação / ✅ Realizado
```

---

**FIM DAS REGRAS v3.0 - CONSULTAR MCP E CONFIRMAR LEITURA ANTES DE COMEÇAR**
