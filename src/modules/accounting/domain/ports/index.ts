/**
 * Domain Ports
 * 
 * Exporta interfaces (Ports) do módulo Accounting
 */

export type {
  IJournalEntryRepository,
  FiscalDocumentData,
  FiscalDocumentItem,
  ChartAccount,
  JournalEntryData,
} from './IJournalEntryRepository';
